#[allow(unused_imports)]
use rust::{Runable, experiment, problems, stds, the_book};

fn main() {
    // stds::Test::run()

    // problems::Solution::run();

    //   the_book::Test::run();

    experiment::Trial::run();
}
